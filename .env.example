# 书签管理后台密码（SHA-256 哈希，非明文）
# 首次启动管理端: vpr dev:admin（无 .env 时从本文件创建，并交互设置密码）
# 主站 + 管理端: vpr dev:all（主站前台，管理端后台 4325）
# 可选: BOOKMARKS_ADMIN_PORT=4325
# 可选: 管理端登录显示名称（登录时可覆盖）
PUBLIC_BOOKMARKS_ADMIN_NAME=admin
PUBLIC_BOOKMARKS_ADMIN_HASH=
