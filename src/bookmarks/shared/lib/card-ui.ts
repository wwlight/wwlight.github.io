/** 书签卡片 nav / admin 共用布局 class */

/** 卡片右上角浮动标签（badgeText） */
export const floatingBadgeClass =
  "absolute -right-1.5 -top-1.5 z-20 shrink-0 shadow-md ring-1 ring-background pointer-events-none";

/** 预览区：左侧站点 icon + 右侧文案（与管理端 BookmarkCardPreview 一致） */
export const bookmarkCardPreviewRowClass =
  "relative z-10 flex h-16 w-full min-w-0 shrink-0 items-center gap-3 overflow-hidden rounded-inherit px-3 py-3";

/** 导航页整卡外壳 */
export const navBookmarkCardShellClass =
  "bookmark-card group relative min-w-0 shrink-0 overflow-visible";
