---
import { flypyFigureClass } from './flypy-classes'

interface Props {
  ariaLabel: string
  class?: string
}

const { ariaLabel, class: className } = Astro.props
---

<figure class={flypyFigureClass(className)} aria-label={ariaLabel}>
  <slot />
</figure>
