---
description: wwlight.github.io 项目约定；每轮任务开始前必须先读项目 skill
alwaysApply: true
---

# wwlight 项目约定

**每轮对话的第一个动作**：用 Read 工具读取 `.cursor/skills/wwlight-project/SKILL.md`，再规划或改代码。不要跳过。

硬性约定（细节见 skill）：

- 安装与脚本：见 [dev-foundation/vite-plus.md](.cursor/skills/dev-foundation/vite-plus.md)（`vp i`、`vpr …`），不要裸跑 `pnpm` / `npm install`
- 依赖版本：只改 `pnpm-workspace.yaml` 的 `catalogs`；`package.json` 用 `catalog:<组名>`
- 改 dev 脚本、Mermaid、Starlight 导航前，对照 skill 对应章节；主题细节以 `src/theme/README.md` 为准；书签细节以 `src/bookmarks/README.md` 为准
- **模块重构 / 结构优化 / 目录重组 / 路由迁移**：用 Read **先读** `.cursor/skills/dev-foundation/SKILL.md`，再读 [`.cursor/skills/wwlight-project/paths.md`](.cursor/skills/wwlight-project/paths.md)；**改目录须同轮更新模块 README 与相关文档**（见 dev-foundation「目录与文档同步」），勿跳过
- 改 Tailwind className 或 `src/styles` 前，读取 [`.cursor/skills/dev-foundation/tailwindcss.md`](.cursor/skills/dev-foundation/tailwindcss.md)（勿对 scale 滥用方括号，如 `p-1` 而非 `p-[1]`）
- 写或改 `src/content/docs/blog/**` 前，读取 `.cursor/skills/dev-foundation/SKILL.md` 的 **技术说明写法**；本书系列目录与 Starlight 格式见 wwlight skill **博客**
- 只在你明确要求时创建 git commit
