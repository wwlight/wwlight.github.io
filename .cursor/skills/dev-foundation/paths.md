# 路径与路由（通用）

**具体 alias / URL 表** → 各仓库 project skill（本书：`wwlight-project/SKILL.md` **路径/路由**）。

| 规则 | |
| --- | --- |
| 一处维护 | alias、pages、例外只在 project skill |
| 重构同轮 | 改 URL → 同步 skill + 模块 README + grep |

模式：`@/` 跨目录；`pages/` 路径 = URL；改路由先 `git mv` pages。

检查：pages↔URL；grep 旧路径；build 产物路由。

流程：[SKILL.md](./SKILL.md)
